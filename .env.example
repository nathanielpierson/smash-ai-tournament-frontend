# Optional. Override the API base URL.
# - Development: defaults to /api (proxied to localhost:3000 by Vite).
# - Production: defaults to https://smash-ai-tournament-backend.onrender.com/api
#
# Uncomment to override (e.g. local backend on different port):
# VITE_API_BASE_URL=http://localhost:3000/api
